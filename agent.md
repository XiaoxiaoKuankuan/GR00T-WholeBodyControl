# Agent 工作与修改记录约束

本文件约束在当前 G1 原生 SONIC 训练分支中工作的所有 Agent，确保代码、训练配置、
验证产物和远端运行状态都有清晰、可追溯的中文记录。

1. 每次修改代码、配置、测试或文档，都必须同步更新根目录的
   `G1_SONIC_修改记录.md`，记录分支、起始 HEAD、修改文件、修改理由、验证结果、
   未运行项目、风险和回滚方法。
2. 新增或修改代码时，新增及改写的注释、docstring 和 TODO 必须使用详细中文；
   不得借机批量改写未涉及的历史注释。
3. 每次修改完成后，必须逐文件暂存并检查 staged diff，使用详细中文提交说明推送到
   当前 feature 分支，再由训练服务器的同名分支执行 `git pull --ff-only`。
4. 用户已有的未提交文件和训练数据均为受保护内容，不得擅自覆盖、清理、stash、reset、
   强制推送或混入无关提交。
5. 为验证代码临时启动的单元测试、冒烟测试、短回合训练、临时 TensorBoard、导出模型、
   日志和 tmux 会话，在记录验证证据后必须精确清理；不得删除正式训练、正式 checkpoint、
   原始数据、测试源码或用户要求保留的产物。
6. 当前 G1 SONIC 八卡训练服务器为 `noetix-12`，代码目录为
   `/root/home/liwei/GR00T-WholeBodyControl`，训练使用 Conda 环境
   `/root/miniconda3/envs/jump`。改变服务器、环境、分支或正式输出目录前必须先核对并记录。
