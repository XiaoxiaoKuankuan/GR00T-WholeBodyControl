# Agent 工作与修改记录约束

本文件约束所有在本仓库内工作的 Agent。其目的，是让 BUMI3 原生 SONIC 集成以及后续修改都有可审计的来源、范围、理由和验证证据，避免代码、配置、机器人资产与训练契约之间出现未记录的漂移。

1. 每次修改代码、配置、机器人资产、测试或文档，都必须同步更新根目录的 `BUMI3_SONIC_修改记录.md`。
2. 每条记录至少包含日期、修改文件、具体改动、修改理由、兼容性影响和实际验证结果。
3. 未运行的测试必须明确记录原因，不能把静态检查描述成仿真或训练已通过。
4. BUMI3 参数只能追溯到 `/home/weili/legged_lab/source/NoetixRobot/NoetixRobot/assets/robots/bumi3/` 当前工作区及用户明确给出的 SONIC 契约；不得与其他 BUMI 版本、OMG、GMR 或其他机器人参数混用。参考工作区若有未提交修改，必须记录文件 SHA256。
5. 修改现有 G1/H2 通用逻辑时，必须保留原默认行为并运行兼容性检查。
6. 新增或修改代码时，所有新增、改写及受本次修改影响的行内注释、块注释、
   docstring 和 TODO 都必须使用详细中文，准确说明代码目的、关键逻辑、输入输出、
   边界条件或必要的设计理由；禁止新增只有英文、中英混写却没有完整中文语义，
   或仅复述代码字面行为的注释。标识符、API 名、张量形状和必要的专有名词可保留原文，
   但解释句必须使用中文。未涉及的历史注释不得借机批量改写，以免混入无关改动。

## Git 分支模型

1. `main` 是生产版本分支，必须始终保持可运行、已验证、可交付。禁止直接在 `main`
   上进行功能开发、算法实验、模块重构或普通参数调整；`main` 只接收验证通过的
   `release/*`，以及用户明确批准的紧急修复。
2. `feature/*` 是功能开发分支，用于新功能、算法研发、模块重构和实验性验证。分支必须
   从最新且工作区状态已核对的 `main` 创建，名称使用小写短横线，例如
   `feature/bumi-native-sonic`；禁止误写为 `featur/*`。一个 feature 分支应尽量只包含一个
   明确主题。
3. `release/*` 是发布准备分支，建议命名为 `release/vX.Y.Z`。该分支只允许发布前测试、
   Bug 修复、参数调整、兼容性修复和文档补充；禁止继续加入大型新功能或大规模重构。
   发布完成后必须把 release 修复同步回仍在开发的相关 feature 分支。
4. `hotfix/*` 用于已发布版本的紧急修复，必须从 `main` 创建。修复验证后合并回 `main`，
   同步到活动的 `release/*` 和受影响的 `feature/*`，并创建补丁版本 Tag。

## Agent 操作与用户修改保护

1. 用户已有的所有未提交修改一律视为正确修改和受保护内容。Agent 不得擅自丢弃、覆盖、
   回退、清理、stash、改写或混入无关提交；即使这些修改不属于当前任务，也必须原样保留。
   若当前任务必须修改同一文件，应先说明重叠范围和处理方式，并保留双方有效内容。
2. 每次修改前必须检查并报告当前分支、HEAD SHA、`git status`、用户未提交修改，以及本地
   分支与远端的领先/落后关系。若位于 `main`，不得直接修改代码，应先获得用户授权并创建
   或切换到合适的 `feature/*`、`release/*` 或 `hotfix/*`。
3. 用户已对“完成当次修改后，在当前开发分支执行非破坏性 commit、push，并在
   `noetix-volc` 的同分支执行 `git pull --ff-only`”给予持续授权，除非用户在某次
   任务中明确要求暂不提交、不推送或不同步服务器。该持续授权不包含 merge、
   rebase、创建或删除 Tag、删除分支、强制推送或改写历史；这些操作仍需用户单独明确授权。
4. 禁止使用 `git reset --hard`、`git clean -fd`、`git push --force` 等破坏性命令；禁止
   改写已推送提交历史。确需例外时，必须先解析精确目标、说明影响和恢复方案，并取得用户
   的单独明确授权。
5. 分支创建、同步、提交等版本管理任务必须和功能实现修改分开执行；用户只要求建立分支
   或同步时，不得顺带修改实现。

## 提交、数据和记录

1. 提交必须小而完整，一个提交只包含一个可解释主题。功能、Bug 修复、重构、测试、配置、
   机器人资产和文档尽量分开提交；禁止无审查使用 `git add .` 或 `git add -A`，必须逐文件
   暂存并检查 staged diff。
2. 提交前必须区分源代码、配置、机器人资产、模型和生成数据。Checkpoint、ONNX、CSV、
   NPZ、PKL、视频、日志、缓存和训练输出不得仅凭扩展名决定是否提交，必须逐文件确认用途；
   大型或生成产物只有在用户明确指定并确定存储策略后才能进入 Git。
3. `BUMI3_SONIC_修改记录.md` 的每项实质修改记录还应包含所属分支、起始 HEAD、配置/资产
   来源与关键 SHA256、测试命令及结果、未运行测试及原因、已知风险、输出产物和回滚方法。
   Git commit message 或聊天记录不能替代仓库内记录。
4. Git 提交消息必须使用详细中文说明。标题可使用 `feat`、`fix`、`refactor`、`test`、
   `docs`、`config`、`asset`、`chore` 等类型前缀，但标题和正文必须清楚说明具体改动、
   修改理由、影响边界、实际验证结果、未执行项及其原因；禁止只写英文短句、模糊描述或
   仅列文件名。
5. 每次完成代码、配置、机器人资产、测试或文档修改后，必须在同一轮任务中完成
   “更新修改记录 -> 运行对应验证 -> 逐文件暂存并检查 staged diff -> 使用详细
   中文消息提交 -> 推送到 GitHub 当前分支 -> 在 `noetix-volc` 同分支执行
   `git pull --ff-only`”闭环，不得把已完成改动长期留在未提交工作区。若本地或服务器
   存在用户未提交改动、分支不一致、非快进历史或网络故障，必须保留现场、记录精确阻塞，
   不得为强行完成闭环而丢弃改动、stash、reset、force push 或自行合并。

## SONIC 训练服务器

1. SONIC 训练统一使用 SSH Host 别名 `noetix-volc`，不得自行替换为其他服务器、IP、
   用户或密钥。连接配置如下：

   ```sshconfig
   Host noetix-volc
       HostName 14.103.42.21
       User root
       Port 22
       IdentityFile ~/.ssh/noetix-8.pem
       IdentitiesOnly yes
       ServerAliveInterval 60
       ServerAliveCountMax 3
   ```

2. 执行训练启动、停止、恢复、数据传输或服务器文件修改前，仍须遵守用户授权范围；仅记录
   服务器配置不代表已授权连接或改变远端状态。

## 验证、合并与发布

1. 验证结论必须明确区分静态检查、单元测试、集成测试、仿真 reset/step、动作 replay、
   训练 smoke 和完整训练/部署验证；低级别检查不得描述为高级别验证通过，未运行项必须说明。
2. Agent 为验证代码而启动的冒烟测试、单元/集成测试运行、短回合训练、临时 replay、
   临时导出或其他一次性验证任务，在测试完成并把命令、结果、关键指标、失败原因和必要的
   复现信息写入 `BUMI3_SONIC_修改记录.md` 后，必须清理该次测试专用的运行目录、临时
   checkpoint、TensorBoard event、导出模型、渲染文件、日志和已结束 tmux 会话，避免
   长期堆积导致正式产物与验证产物混淆。删除前必须确认相关进程已经结束，解析并逐项核对
   精确目标，确认目标仅属于该次测试且未被正式训练、其他进程或用户任务引用；禁止使用
   宽泛目录、未解析变量、递归通配符或 `git clean` 清理。不得删除测试源码、fixture、
   原始数据、正式训练目录、正式 checkpoint、用户产物或共享缓存。用户明确要求保留某项
   测试产物时，以该次要求为准，并在修改记录中说明保留路径和理由。
3. feature 合入 release、release 合入 main 前，必须先核对完整 diff、变更记录、测试证据、
   已知限制和回滚方法，并获得用户明确授权。共享分支不得随意 rebase；合并应保留可审计的
   分支和提交边界。
4. 正式版本只允许在 `main` 的已验证提交上创建 annotated Tag，推荐语义版本
   `vMAJOR.MINOR.PATCH`；候选版本使用 `vX.Y.Z-rc.N`。Tag 说明必须包含主要功能、修复、
   关键配置/资产/Checkpoint 来源、验证结果和已知限制；禁止移动、覆盖或复用已发布 Tag。
5. GitHub 应为 `main` 配置禁止直接 push、禁止 force push、禁止删除、必须通过状态检查和
   PR 审核等分支保护；`release/*` 建议采用同等保护。Agent 规则是操作约束，不能替代远端
   分支保护。
